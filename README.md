# cursor-cloud-home

Private mirror of Mac `~/.cursor/skills` for Cursor Cloud Agent VMs.

- **SSOT on Mac:** `~/.cursor/skills`
- **Sync:** `~/.cursor/bin/sync-cloud-home.sh`
- **Cloud install:** `./install-on-cloud.sh` (called from environment install)

Do not put secrets here.
