# Cloud delivery via environment install, not Teams

Stay on a personal Cursor plan. Cloud Agents get pack skills by cloning this private repo in `.cursor/environment.json` `install` (secret `CURSOR_CLOUD_HOME_TOKEN`) and copying into the VM’s `~/.cursor/skills/`. Rejected: Team Marketplace Required plugins (needs Teams/Enterprise) and relying on IDE marketplace installs (do not sync to Cloud VMs).
