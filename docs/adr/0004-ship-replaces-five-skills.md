# One /ship skill replaces five review/verify skills

Merge gauntlet, code-review, pr-review, address-pr-comments, and thermos into a single `/ship` skill with modes (`gauntlet`, `review`, `thermos`, `address`, `green`). Delete the old skill names from the pack and from the laptop with no alias stubs. Tradeoff: thicker skill, sharper modes required; gain: one entrypoint for “make it green / check it / audit it.”
