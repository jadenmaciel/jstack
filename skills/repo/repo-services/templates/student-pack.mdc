---
description: Student Pack tools wired for this repo (local + Cloud)
globs:
alwaysApply: true
---

# Student Pack — REPO_NAME

Doppler project: **DOPPLER_PROJECT** / config `dev`.

## Secrets

- Humans: 1Password
- App/runtime: Doppler (`doppler.yaml`, `doppler run --`)
- Cloud: Cursor dashboard Runtime Secrets (see `~/.cursor/docs/cloud-agent-dashboard.md`)

Never commit secrets. epayment/TROUTE is out of scope.

## Tier: TIER

MCP allowlist for Cloud Agents matches `.cursor/environment.json`.

## Skip on this repo

See `docs/agents/dev-tooling.md` or `docs/STUDENT_PACK.md`.
