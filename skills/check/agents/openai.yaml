interface:
  display_name: "Check"
  short_description: "Validate with automatic two-axis review"
  default_prompt: "Use $check to validate this completed implementation."
policy:
  allow_implicit_invocation: true
