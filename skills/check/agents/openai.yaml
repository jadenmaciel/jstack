interface:
  display_name: "Check"
  short_description: "Gauntlet + thermos ship gate with fix loop"
  default_prompt: "Use $check to validate this completed implementation."
policy:
  allow_implicit_invocation: true
