interface:
  display_name: "Epayment Check"
  short_description: "Validate epayment with two exact-tree reviews"
  default_prompt: "Use $epayment-check to validate this epayment implementation."
policy:
  allow_implicit_invocation: false
