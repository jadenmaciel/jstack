interface:
  display_name: "Fix"
  short_description: "Repair findings from the latest check"
  default_prompt: "Use $fix to repair every structured finding from the latest $check or $epayment-check."
policy:
  allow_implicit_invocation: true
