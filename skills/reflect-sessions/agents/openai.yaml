interface:
  display_name: "Reflect Sessions"
  short_description: "Audit agent config and rank setup improvements from session evidence"
  default_prompt: "Use $reflect-sessions to run the read-only agent-config audit of Codex and Claude Code skills, plugins, MCP servers, hooks, and persistent instructions, then reflect on recent sessions for recurring setup improvements."
policy:
  allow_implicit_invocation: false
